doctype html
html(lang="de")
  head
    meta(charset"utf-8")
    meta(name="viewport" content="initial-scale=1")
    link(rel="stylesheet" href="/lib/bootstrap/dist/css/bootstrap.min.css")
    title= title
  body
    h1= title
    #container
      table.table.table-hover.table-condensed
        thead
          tr: th(colspan="2") Farbt&ouml;ne
          tr
            th Farbcode
            th Farbton
        tbody
          each color in colors
            tr
              td= color.Farbcode
              td= color.Farbton
